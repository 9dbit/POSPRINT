const rupiah=n=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(Number(n||0));
let meta={products:[],addons:[]};
async function get(url,opts){const r=await fetch(url,opts);const j=await r.json();if(!r.ok)throw new Error(j.error||'Request failed');return j}
async function load(){meta=await get('/api/meta');const d=await get('/api/dashboard');const orders=await get('/api/orders');document.querySelector('#product').innerHTML=meta.products.map(p=>`<option value="${p.id}">${p.name} • ${rupiah(p.sell_price)}/${p.unit}</option>`).join('');document.querySelector('#addons').innerHTML=meta.addons.map(a=>`<label class="check"><input type="checkbox" value="${a.id}" data-price="${a.sell_price}"><span>${a.name}<br><small>${a.category} • ${rupiah(a.sell_price)}</small></span></label>`).join('');document.querySelector('#cards').innerHTML=`<div class="card"><div class="label">Active Jobs</div><div class="value">${d.orders.active}</div></div><div class="card"><div class="label">Paid Revenue</div><div class="value">${rupiah(d.finance.revenue)}</div></div><div class="card"><div class="label">Estimated Gross Profit</div><div class="value">${rupiah(d.finance.gross_profit)}</div></div><div class="card"><div class="label">Low Stock SKU</div><div class="value">${d.stock.low_stock}</div></div>`;document.querySelector('#orders').innerHTML=orders.map(o=>`<tr><td><strong>${o.order_no}</strong></td><td>${o.customer_name}</td><td><span class="badge ${o.status}">${o.status}</span></td><td>${rupiah(o.grand_total)}</td><td>${rupiah(o.cost_estimate)}</td><td>${rupiah(Number(o.grand_total)-Number(o.cost_estimate))}</td><td>${new Date(o.created_at).toLocaleString('id-ID')}</td></tr>`).join('');document.querySelector('#inventory').innerHTML=meta.products.map(p=>`<tr><td>${p.sku}</td><td>${p.name}</td><td>${p.material_category}</td><td>${p.machine_category}</td><td><strong>${p.stock_qty} ${p.unit}</strong></td><td>${p.min_stock}</td><td>${rupiah(p.sell_price)}</td></tr>`).join('');estimate()}
function estimate(){const p=meta.products.find(x=>String(x.id)===document.querySelector('#product').value);let t=p?Number(p.sell_price)*Number(document.querySelector('#qty').value||0):0;document.querySelectorAll('#addons input:checked').forEach(x=>t+=Number(x.dataset.price));document.querySelector('#estimate').textContent=`Estimated total: ${rupiah(t)}`}
document.addEventListener('change',e=>{if(e.target.matches('#product,#qty,#addons input'))estimate()});
document.querySelector('#createOrder').onclick=async()=>{try{const addon_ids=[...document.querySelectorAll('#addons input:checked')].map(x=>Number(x.value));await get('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({customer_name:document.querySelector('#customer').value||'Walk-in',customer_phone:document.querySelector('#phone').value,items:[{product_id:Number(document.querySelector('#product').value),qty:Number(document.querySelector('#qty').value||1)}],addon_ids,actor:'Admin POS'})});await load()}catch(e){alert(e.message)}};
document.querySelector('#addProduct').onclick=async()=>{try{await get('/api/products',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({sku:sku.value,name:pname.value,material_category:material.value,machine_category:machine.value,unit:unit.value,cost_price:cost.value,sell_price:sell.value,stock_qty:stock.value,min_stock:0})});await load()}catch(e){alert(e.message)}};
load();
